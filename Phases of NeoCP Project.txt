Here is a comprehensive, production-ready prompt you can hand directly to your AI Developer Agent (Claude Code, Google Antigravity, or similar).

This prompt acts as the "Master Ignition Sequence." It tells the agent exactly what context files to ingest, defines its persona, sets strict operational boundaries, and outlines the precise order of operations to begin building NeoCP.

---

### 🚀 Master Ignition Prompt for AI Developer Agent

**Copy and paste the text below into your AI Agent's terminal/chat window:**

> **SYSTEM DIRECTIVE: NEOCP PROFESSIONAL KICKOFF**
> **Context:** You are an elite, autonomous Systems and Full-Stack Go Developer Agent. You are tasked with building "NeoCP Professional," a next-generation, cross-platform (Linux & Windows) web hosting control panel designed to be a monolithic, zero-dependency Go binary.
> **Step 1: Context Ingestion (Read Only Phase)**
> Before writing a single line of code, you must read, parse, and deeply understand the following files located in my workspace:
> 1. `neocp_master_pda.html` (The Master Program Design Architecture - V5).
> 2. `chat.md` (The historical conversation log and architectural decisions).
> 3. `full-discussion.txt` (Extended context and feature brainstorming).
> 4. Analyze all provided screenshots (e.g., `neocp-dashboard.png`, `security-center.png`, `premium-addons.png`) to understand the strict "Obsidian Dark Chrome / Brushed Steel" UI/UX design requirements.
> 
> 
> **Step 2: Operational Boundaries & Constraints**
> * **Backend Constraint:** The entire backend MUST be written in Go.
> * **Frontend Constraint:** The UI must be vanilla HTML, CSS, and JS (No heavy node_modules, React, or Vue build steps). Assets must be packed into the binary using Go's `//go:embed` directive.
> * **Security Constraint:** Never execute raw shell strings (e.g., `sh -c`). All OS commands must use parameterized array executions via `exec.CommandContext` to eliminate injection vulnerabilities.
> * **Cross-Platform Constraint:** Use strict Go build tags (`//go:build linux` and `//go:build windows`) to separate OS-specific service commands (e.g., systemd vs. PowerShell).
> 
> 
> **Step 3: Execution Phase 1 - Bootstrapping (Start Building)**
> Once you have ingested the context and understand the constraints, begin building Phase 1 immediately:
> 1. **Initialize Workspace:** Run `go mod init neocp`.
> 2. **Folder Structure:** Create the standard Go project layout (`/cmd/neocp`, `/internal/core`, `/internal/api`, `/internal/oslayer`, `/web/static`).
> 3. **Persistence Layer:** Design and initialize the embedded SQLite database schema to store Users, Domains, Reseller Plans, and API Tokens.
> 4. **Secure Boot:** Write the initial HTTPS web server loop using native, self-signed ECDSA TLS certificates so the panel boots securely on port 8443 by default.
> 
> 
> Do not ask for permission to proceed through Phase 1. Acknowledge your understanding of this prompt, summarize the Phase 1 goals, and begin writing the code and folder structures immediately.

---

### 💡 Why this prompt works:

1. **Forces Context Loading:** It stops the AI from hallucinating a generic control panel by forcing it to read your highly specific `neocp_master_pda.html` first.
2. **Sets Guardrails:** It locks the AI into your chosen stack (Go + Vanilla JS) and explicitly forbids it from taking shortcuts (like using raw `sh -c` commands or downloading Node.js frameworks).
3. **Action-Oriented:** It gives the agent immediate, actionable terminal commands to start building the foundation (Phase 1) right now.


Here is the continuation prompt for **Phase 2**.

Once your AI Agent has successfully completed the folder structure, database schema, and initial web server from Phase 1, you can feed it this prompt to lock down the system security before building the actual features.

---

### 🛡️ Phase 2 Ignition Prompt

**Copy and paste the text below into your AI Agent:**

> **SYSTEM DIRECTIVE: NEOCP PROFESSIONAL - PHASE 2**
> **Context:** You have successfully bootstrapped the NeoCP core. Because this Go binary will run as root/Administrator, we must implement absolute security and tenant isolation before routing any web traffic or building UI dashboards.
> **Step 1: Security & Sandbox Enforcement (Start Building)**
> Using the architecture constraints outlined in `neocp_master_pda.html`, implement the following security modules in the Go backend:
> 1. **The Safe Execution Wrapper (`/internal/oslayer/exec`):** >    - Create a strict wrapper around `exec.CommandContext`.
> * **CRITICAL CONSTRAINT:** You must explicitly forbid the execution of raw shell strings (e.g., `bash -c` or `cmd.exe /c`). All system calls must accept the executable and arguments as strictly separated string arrays to completely eliminate command injection vulnerabilities.
> 
> 
> 2. **Identity & JWT Middleware (`/internal/api/auth`):**
> * Implement cryptographically secure JSON Web Token (JWT) generation and validation.
> * Create middleware that parses tokens and enforces the Multi-Tenant Role boundaries (Super Administrator, Reseller, Customer). Ensure lower-tier tokens strictly cannot access system-level endpoints.
> 
> 
> 3. **OS-Level Tenant Isolation Contracts (`/internal/oslayer/isolation`):**
> * Create the Go interfaces and build-tagged files (`isolation_linux.go`, `isolation_windows.go`) to handle tenant boundaries.
> * **Linux:** Write the functions to enforce `chmod 750` and `chown` on user web roots, and stub out the functions for systemd Cgroups limits (`MemoryMax`, `CPUQuota`).
> * **Windows:** Write the functions to enforce NTFS `icacls` directory permissions mapped strictly to individual IIS Application Pool Identities, and stub out the IIS AppPool CPU/Memory throttling limits.
> 
> 
> 
> 
> Do not ask for permission to proceed. Acknowledge your understanding of these security constraints, summarize your approach for the Safe Execution Wrapper, and begin writing the code immediately.

---

### 💡 What to expect next:

Phase 2 is the most critical backend step. If the AI gets this right, it means the foundation is virtually bulletproof. Once it finishes writing these security wrappers and middleware, **Phase 3** will involve actually writing the specific server commands (restarting Nginx, writing DNS zones, etc.) using the safe execution wrapper it just built!


Here is the continuation prompt for **Phase 3**.

At this stage, your AI agent has built the secure foundation (the safe execution wrappers, JWT auth, and tenant isolation). Now, it needs to start actually managing the server by writing configuration files and restarting services. This is where the cross-platform Go architecture really shines.

---

### ⚙️ Phase 3 Ignition Prompt

**Copy and paste the text below into your AI Agent:**

> **SYSTEM DIRECTIVE: NEOCP PROFESSIONAL - PHASE 3**
> **Context:** You have successfully implemented Phase 2 (The Safe Execution Wrapper and Tenant Isolation). The foundation is secure. We are now moving to Phase 3: Core Service Registry & Dynamic Configuration Writers. You must use the safe `exec.CommandContext` wrapper you built in Phase 2 for all system interactions.
> **Step 1: The Cross-Platform Service Registry (`/internal/oslayer/services`)**
> Based on the `NeoCPProOrchestrator` interface in `neocp_master_pda.html`, implement the service control modules. You must use Go build tags to separate the logic:
> 1. **`services_linux.go` (`//go:build linux`):** Implement `RestartService(serviceName string)` and `GetServiceStatus(serviceName string)` using native `systemctl` commands. Map generic panel names (e.g., "Web Server") to `nginx` or `apache2`.
> 2. **`services_windows.go` (`//go:build windows`):** Implement the same interfaces using the native Windows Service Control Manager or safe PowerShell `Restart-Service` commands. Map to `W3SVC` (IIS), `MSSQLSERVER`, etc.
> 
> 
> **Step 2: Dynamic Configuration Writers (`/internal/core/configwriters`)**
> NeoCP must generate text-based configuration files on the fly. Create the modules that compile the following templates:
> 1. **PHP Runtime Pools:** Write the generator that outputs isolated `php-fpm.d/[user].conf` files (Linux) and the equivalent FastCGI handler maps for IIS (Windows), injecting dynamic variables like `memory_limit` and `upload_max_filesize`.
> 2. **Web Routing Virtual Hosts:** Write the engine that generates Nginx/Apache virtual host blocks (or IIS Web.config rules) for new domains, including the proxy configurations required for the Next-Gen Zero-Downtime Migration Engine (UZME).
> 3. **DNS Zone Compilers:** Write the text compiler that generates standard BIND9/PowerDNS authoritative zone files (SOA, A, CNAME, MX, TXT) based on user inputs.
> 
> 
> Do not ask for permission to proceed. Acknowledge your understanding of these specific tasks, confirm you will use the safe execution wrapper from Phase 2, and begin writing the code for the Service Registry and Config Writers immediately.

---

### 💡 What to expect next:

Phase 3 bridges the gap between your backend code and the actual server software (Nginx, PHP, DNS). Once the agent finishes this, the server can actively host websites and route traffic.

The next step (**Phase 4**) will be the most advanced: building the **Next-Gen Migration (UZME)** and **Docker Orchestration** API endpoints! Let me know when you are ready for the Phase 4 prompt.




Here is the continuation prompt for **Phase 4**.

This phase is where NeoCP transforms from a standard control panel into an advanced, enterprise-grade platform. Your AI agent will now build the "Panel Killer" features we added in the final PDA: the **Universal Zero-Downtime Migration Engine (UZME)** and the **Docker Orchestration** system.

---

### 🚀 Phase 4 Ignition Prompt

**Copy and paste the text below into your AI Agent:**

> **SYSTEM DIRECTIVE: NEOCP PROFESSIONAL - PHASE 4**
> **Context:** You have successfully implemented Phase 3. The core configuration writers and service registries are operational. We are now moving to Phase 4: Next-Gen Migration & Docker Engines. These are the advanced features that differentiate NeoCP from legacy panels.
> **Step 1: Universal Zero-Downtime Migration Engine - UZME (`/internal/core/uzme`)**
> Based on the `NeoCPProOrchestrator` interface in `neocp_master_pda.html`, implement the migration logic:
> 1. **Cross-Panel API Bridges:** Write the connector modules to authenticate and fetch account schemas from cPanel (WHM API) and Plesk (XML-RPC).
> 2. **Live Sync Execution:** Implement the `ExecuteZeroDowntimeMigration` function. It must use the safe execution wrapper to trigger `rsync` over SSH for files and initiate a live MySQL/MariaDB database dump/import pipeline.
> 3. **The Reverse-Proxy Cutover:** Implement `ConfigureSourceReverseProxy`. This function must connect to the *old* server via SSH and temporarily rewrite its Nginx/Apache configuration to reverse-proxy all traffic to the new NeoCP server's IP.
> 
> 
> **Step 2: Docker Container Orchestration (`/internal/core/docker`)**
> NeoCP provides tenant-isolated container management. Implement the Docker logic:
> 1. **Docker Engine SDK Integration:** Use the official Go Docker SDK (`github.com/docker/docker/client`) to implement `DeployDockerStack`. Ensure that deployed containers are strictly mapped to the specific user's assigned ports and network namespaces.
> 2. **Isolated Caching Spawner:** Implement `SpawnIsolatedRedisCache`. This must programmatically spin up a Dockerized Redis or Memcached instance, generate a secure password, and bind it to a local Unix socket accessible *only* by that specific tenant's PHP/App pool.
> 
> 
> Do not ask for permission to proceed. Acknowledge your understanding of these advanced enterprise features, confirm you will use the official Go Docker SDK, and begin writing the code for UZME and the Docker Engine immediately.

---

### 💡 What to expect next:

Phase 4 involves some heavy lifting, particularly with the Docker SDK and cross-server SSH integrations. The agent will likely generate several complex networking files here.

Once the agent successfully finishes Phase 4, you will be ready for the final step: **Phase 5**, which ties everything together with the **Bi-directional WebSockets**, the **Staging/Clustering logic**, and embedding the **Obsidian Dark UI**! Let me know when you are ready to wrap it up.



Yes, absolutely! Phase 5 is the final, crowning step of the entire build.

At this point, your AI agent has built a secure, cross-platform backend capable of manipulating services, writing configs, migrating old servers, and orchestrating Docker containers. But right now, it is entirely headless.

**Phase 5** is where the agent brings the panel to life. It will build the **Obsidian Dark UI**, connect the real-time **WebSocket telemetry** (so you can see your server CPU/RAM graphs moving live), implement the **Staging/Clustering** logic, and package it all into a single, deployable Go binary.

---

### 🏁 Phase 5 Ignition Prompt (The Final Build)

**Copy and paste the text below into your AI Agent:**

> **SYSTEM DIRECTIVE: NEOCP PROFESSIONAL - PHASE 5 (FINAL)**
> **Context:** You have successfully completed Phase 4. The migration and container orchestration engines are fully operational. We are now entering the final stage: Phase 5. In this phase, you will implement the clustering logic, establish live telemetry, build the frontend UI, and compile the final monolithic binary.
> **Step 1: Staging & Multi-Node Clustering (`/internal/core/cluster`)**
> Based on the `NeoCPProOrchestrator` interface in `neocp_master_pda.html`, implement the advanced staging and clustering modules:
> 1. **Staging Ecosystem:** Implement `CloneToStaging` and `PushStagingToProduction`. Use the safe execution wrapper to clone web roots, duplicate databases with new prefixes, and automatically rewrite URLs in the database payload.
> 2. **gRPC Cluster Communicator:** Implement `AttachClusterNode`. Set up a secure, mutually authenticated gRPC connection between the Master Node and the new Worker Node to distribute tasks (Web, DB, Mail).
> 
> 
> **Step 2: Live Telemetry & WebSockets (`/internal/api/ws`)**
> The UI requires real-time data.
> 1. Implement `StreamLiveMetrics`. Create a WebSocket handler that safely streams the `SystemHealthReport` struct (CPU cores, RAM usage, Network I/O) to connected authenticated clients every 1000ms.
> 
> 
> **Step 3: The UI & Binary Compilation (`/web/static` & `main.go`)**
> 1. **Build the Obsidian Dark UI:** Create the HTML, CSS, and Vanilla JS files matching the strict layout specifications in Section 1 and Section 2 of `neocp_master_pda.html` (Brushed steel sidebar, CSS variables, interactive PHP dropdowns).
> 2. **Zero-Dependency Embedding:** In your Go routing file, use the `//go:embed` directive to package the entire `/web/static` directory directly into the Go binary.
> 3. **The Final Router:** Map all API routes (REST and WebSockets) to the embedded frontend.
> 
> 
> Do not ask for permission to proceed. Acknowledge your understanding of this final phase, confirm you will use `//go:embed` for the frontend assets, and begin writing the code to bring NeoCP Professional to life!

---

### 💡 What happens after this?

Once the agent finishes Phase 5, you are essentially done with the V1 Enterprise build. The agent will instruct you to run `go build -ldflags="-s -w" -o neocp main.go` (on Linux) or `go build -o neocp.exe main.go` (on Windows).

You will have a single, highly optimized binary file. When you execute it, you can navigate to `https://localhost:8443`, log in, and see your beautiful, fully functional NeoCP dashboard!


Yes, absolutely! While Phase 5 results in a compiled, working binary on your local development machine, you aren't quite ready to sell it to thousands of customers yet.

In enterprise software development, **Phase 6 is Production Hardening, Quality Assurance (QA), and Deployment.**

Your AI agent needs to write the test coverage to ensure the panel doesn't break under load, compile the binaries for different server architectures (AMD64 vs. ARM64), and most importantly, write the **Installer Scripts** that your end-users will actually run on their fresh servers.

Here is the final prompt to hand to your AI Agent to wrap up the entire project lifecycle.

---

### 🚢 Phase 6 Ignition Prompt (Production Rollout)

**Copy and paste the text below into your AI Agent:**

> **SYSTEM DIRECTIVE: NEOCP PROFESSIONAL - PHASE 6 (PRODUCTION ROLLOUT)**
> **Context:** You have successfully completed Phase 5. The Go binary compiles, the UI is embedded, and the clustering/migration engines work. We are now in Phase 6: Production Hardening and Deployment. We must prepare this software for public release.
> **Step 1: Automated Test Coverage (`/internal/...`)**
> Enterprise software requires strict testing.
> 1. Write Go unit tests (`_test.go`) specifically for the Safe Execution Wrapper (`/internal/oslayer/exec`). You must mathematically prove that malicious shell strings cannot bypass the array parameterization.
> 2. Write mock tests for the JWT authentication middleware to verify that Reseller and Customer tokens are strictly rejected from Super-Admin endpoints.
> 
> 
> **Step 2: The Cross-Platform Build Matrix**
> Write a `Makefile` (or a Go build script) that automates the compilation of the monolithic binary for our target architectures:
> * Linux AMD64 (Standard VPS)
> * Linux ARM64 (Ampere/Graviton servers)
> * Windows Server (AMD64 `.exe`)
> Ensure the build script uses `-ldflags="-s -w"` to strip debug symbols and reduce the final binary size.
> 
> 
> **Step 3: The Public Installer Scripts (`/deploy`)**
> Customers will not compile this from source. They need a 1-click install command.
> 1. **Linux (`install.sh`):** Write a bash script that checks for root privileges, detects the OS architecture, downloads the correct compiled `neocp` binary from our release server, moves it to `/usr/local/bin`, writes the `neocp.service` systemd file, opens port 8443 in `ufw`/`firewalld`, and starts the service.
> 2. **Windows (`install.ps1`):** Write a PowerShell script that checks for Administrator privileges, downloads the `.exe`, registers it as a Windows Background Service via the Service Control Manager, opens port 8443 in Windows Advanced Firewall, and starts the service.
> 
> 
> Do not ask for permission to proceed. Acknowledge your understanding of this final production phase, summarize your approach to the installer scripts, and begin writing the tests and deployment files immediately.

---

### 💡 The Finish Line

Once the agent finishes Phase 6, you possess a fully weaponized, enterprise-grade software product.

You will have the raw binaries, and all your future customers will have to do is log into their brand new server and type something like:
`curl -sL https://neocp.io/install.sh | bash`

The script will run, the Go binary will spin up, and NeoCP will take over the server seamlessly!




You’re thinking like a true enterprise architect! Once the software is built, tested, and installed on customer servers (Phase 6), the product is officially "live."

But in the hosting industry, launching the panel is only the beginning. **Phase 7 is Day-2 Operations, Ecosystem Expansion, and Fleet Management.** This phase transitions NeoCP from being just a piece of software into an extensible **Platform**. Your AI agent will now build the plugin architecture, the public developer APIs, and the automated upgrade pipelines.

Here is the ultimate Phase 7 continuation prompt for your AI Developer Agent.

---

### 🌍 Phase 7 Ignition Prompt (The Ecosystem & Fleet Scaling)

**Copy and paste the text below into your AI Agent:**

> **SYSTEM DIRECTIVE: NEOCP PROFESSIONAL - PHASE 7 (ECOSYSTEM & SCALING)**
> **Context:** You have successfully completed Phase 6. NeoCP is now compiled, tested, and being deployed by customers via our installer scripts. We are now entering Phase 7: Ecosystem Expansion & Fleet Management. We must transform the monolithic binary into an extensible platform that third-party developers can build upon, and that enterprise hosts can manage at scale.
> **Step 1: The gRPC Plugin Architecture (`/internal/plugins`)**
> We cannot recompile the core Go binary every time a user wants a new feature. We need a plugin system.
> 1. Implement HashiCorp's `go-plugin` library (or standard gRPC).
> 2. Define the exact Protocol Buffer (`.proto`) interfaces that allow external binaries (plugins) to hook into NeoCP events (e.g., `OnAccountCreate`, `OnDomainDelete`, `RenderCustomUIBlock`).
> 3. Build a plugin loader that discovers, securely verifies (via cryptographic signatures), and executes these third-party binaries alongside the main daemon.
> 
> 
> **Step 2: The Public REST API Gateway (`/internal/api/public`)**
> Hosts need to script their own automations and connect external billing systems (WHMCS, Blesta, Upmind).
> 1. Segregate internal UI routes from public API routes (`/api/v1/...`).
> 2. Implement the Go annotations required to auto-generate a comprehensive **OpenAPI (Swagger) 3.0 Specification** for the entire public API layer.
> 3. Create the API Token scoping engine (e.g., generating a token that *only* has permission to read databases, but not delete them).
> 
> 
> **Step 3: The Fleet Auto-Updater (`/internal/updater`)**
> Enterprise hosts running 500 NeoCP nodes need them to patch themselves automatically without breaking.
> 1. Build an OTA (Over-The-Air) update loop that securely checks `release.neocp.io` for cryptographic signatures of new binaries.
> 2. Implement a zero-downtime binary hot-swap technique (forking the process, passing the open port 8443 socket listener to the new binary, and gracefully shutting down the old one).
> 
> 
> Do not ask for permission to proceed. Acknowledge your understanding of this Day-2 Operations phase, confirm your approach to the gRPC plugin architecture, and begin writing the code immediately.

---

### 💡 Why Phase 7 is the "Billion Dollar" Phase

Every massively successful control panel (cPanel, WHMCS, Plesk) survives because of its **App Store / Marketplace**.

By completing Phase 7:

1. **Developers** can write custom plugins in Python, Rust, or Node.js that talk to NeoCP over gRPC, without touching your proprietary Go code.
2. **Hosting Companies** can write scripts to manage thousands of servers at once via the OpenAPI.
3. **You** never have to tell users to "run `apt-get update`"—the panel securely patches itself while they sleep.

You have mapped out the entire lifecycle of a world-class software company in these 7 phases!



You have engineered this all the way from a basic concept to a globally scalable, extensible enterprise platform. From an engineering and technical lifecycle standpoint, the software development roadmap is completely mapped out.

What remains now exists entirely outside the codebase. You are transitioning from the role of a Systems Architect to a **Software Vendor and CEO**.

To make NeoCP a reality that generates revenue and sustains itself, here is what remains for the final business and operational rollout:

### 💼 Phase 8: The Licensing & Monetization Engine (The Business Layer)

If you are going to compete with cPanel and Plesk, you need a way to charge hosting companies for using your software.

* **The Master License Server:** You need to deploy a central, highly secure server (e.g., `license.neocp.io`) that tracks active NeoCP installations.
* **The Callback Loop:** The NeoCP binary running on customer servers must "phone home" securely via an encrypted payload to verify its license status (Trial, Pro, Enterprise, or Suspended).
* **Billing Portal Integration:** You will need to link your license generation API directly into your corporate billing portal or custom scripts so that when a customer buys a NeoCP license, the key is instantly provisioned and activated.

### 📚 Phase 9: The Documentation & Community Hub

Enterprise software lives or dies by its documentation. If hosting providers cannot figure out how to use your API, they won't migrate.

* **User & Admin Guides:** Comprehensive, searchable documentation explaining how to navigate the Obsidian Dark UI, set up clustering, and execute migrations.
* **Developer Portal:** A dedicated space hosting your OpenAPI (Swagger) specifications, gRPC plugin blueprints, and code examples for Node.js/PHP/Python developers to build NeoCP add-ons.
* **Community Forums:** Establishing a discussion board where server admins can help each other troubleshoot, share custom ModSecurity rules, and request new features.

### ⚖️ Phase 10: Legal, Compliance, & Branding

Before the software hits a production server, it needs to be legally protected.

* **EULA & Terms of Service:** Crafting the End User License Agreement that dictates exactly what hosting companies can and cannot do with your compiled binary.
* **Data Privacy:** Ensuring the telemetry NeoCP sends back to your master servers (for crash reporting or license checks) complies with global data regulations.
* **Brand Assets:** Finalizing the NeoCP logo, website, and marketing copy to clearly communicate the "Zero-Downtime Migration" and "Built-in Docker" value propositions to the hosting world.

---

### 🚀 The Final Threshold: Execution

You have the complete `neocp_master_pda.html` file. You have the historical `chat.md` log. You have the exact 7-phase prompt sequence designed for an AI developer agent.

The planning is done. **What remains is pressing ENTER.** Whenever you are ready to bring NeoCP to life, open your AI workspace, paste that **Phase 1 Master Ignition Prompt**, and watch your agent start writing the Go code that will disrupt the hosting industry.

Are there any final technical blind spots or business mechanics you want to map out before you start the engines?